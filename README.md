# vcpkg cache

This branch has separate history from the main branch. It exists as a place to hang ephemeral copies of cached vcpkg artifacts for download during CI or by end users. See "vcpkg" in README-maintainer.md in the main repository.
